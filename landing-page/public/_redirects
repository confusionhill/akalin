# Cloudflare Pages / Workers static _redirects file
# Note: Source paths in _redirects must be relative paths (e.g. /old-path /new-path 301).
# Domain canonical redirects (e.g. www -> non-www or http -> https) are managed by Cloudflare SSL/TLS & Redirect Rules in the Cloudflare Dashboard.
